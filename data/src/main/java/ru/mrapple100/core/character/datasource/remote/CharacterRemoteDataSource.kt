package ru.mrapple100.core.character.datasource.remote

import javax.inject.Inject

internal class CharacterRemoteDataSource @Inject constructor(){
    suspend fun getCharacters(from:Int,to:Int) {
        //TODO
    }
}